import "./assets/css/main.css";
import $ from 'jquery';
import slick from 'slick-carousel';
$('.gallery').slick({
    prevArrow: false,
    nextArrow: false,
    dots: true
})
$('.clients').slick({
    prevArrow: false,
    nextArrow: false,
    dots: true,
    fade: true
})
